You are doing a quick fix for this Flutter project.

## Issue

$ARGUMENTS

## Protocol

This is a FAST fix — minimal changes, no planning overhead.

### 1. Understand

Read relevant code. Identify the root cause quickly.

### 2. Fix

Make the minimal change needed. Follow rules in `CLAUDE.md`.

### 3. Validate

```
analyze_files → dart_fix → dart_format
```

### 4. Test (if applicable)

Run related tests only:
```bash
flutter test test/path/to/related_test.dart
```

### 5. Report

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
QUICK FIX COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Issue: <description>
Fix: <what was changed>
Files: X modified
Tests: passed/skipped
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 6. Next Step

> Fix complete. Run `/commit` to commit changes?
